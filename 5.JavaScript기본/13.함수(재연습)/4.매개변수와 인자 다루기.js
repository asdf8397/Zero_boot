/** 함수에서 매개변수의 인자 다루기
 *
 * 함수에서
 * fucntion func(매개변수[paramter]) {
 *      return 매개변수[parameter]
 * }
 * func(인자[argument]);
 */

function func(parameter) {
    return parameter
}
console.log(func("여기는 에베레스트산 입니다 해발고도는 모릅니다."));