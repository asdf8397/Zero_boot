/** 5. 식별자
 * 식별자는 코드 내의 변수, 함수 혹은 속성(en-US)를 식별하는 문자열입니다.
 *
 * 식별자(aka 예약어)
 * 1. 문자열 시작
 * 2. 언어의 내장 예약어와 중복을 피해야 함
 */

function function() {
    /* 언어는 중복되면 안된다 특히 예약어를 선언해서 써주면 안된다
    break, let, const, var, function, for, default, break, continue
    등등처럼 */
} 