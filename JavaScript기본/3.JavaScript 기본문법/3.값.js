/* 값 */
// 더 이상 평가 될  수 없는 하나의 식으로 ___ 자료형을 간단한 이루는 값

// 자료형(Data Type)
/*
1 숫자형
true 블리언(논리형)
A 문자
ABC 문자열
*/

// 문자열 값(원시 값)
var str = "apple";

Big_English = str.toUpperCase(); // toUpperCase() 대문자로 바꿈

console.log(str.toUpperCase());
console.log(Big_English);

const un = undefined;
const nu = null; 
const bool = false;
const num = '0';
const str = '문자열';

if (num === 0) {
    console.log('num은 0입니다.')
} else {
    console.log('num은 0이 아닙니다.')
};