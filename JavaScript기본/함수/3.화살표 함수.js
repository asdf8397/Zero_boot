/* 3. 화살표 함수 */

// 화살표 함수
const arrowFunc = (nanana) => { 
    // 화살표 함수는 function을 빼고 편리하게 사용할 수 있음
    // 화살표 함수는 함수 내에 여러개가 들어갈때 편리하게 사용할 수 있음
    /**
     * JS의 화살표 함수는 동작 방식이 기존의 function 함수들과 많이 다르다
     * 1. argument
     * 2. this
     */ 

    return nanana + "문자열" + "여러가지를 넣을수 있지요"
}
console.log(arrowFunc("함수의 "));